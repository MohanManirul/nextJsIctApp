import React from 'react';
import Menu from '../components/Menu';

const page = () => {
    return (
        <div>
            <Menu />
            <p>mission text mission text mission text mission text mission text mission text mission text </p>
        </div>
    );
};

export default page;